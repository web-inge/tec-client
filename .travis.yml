language: node_js
node_js:
  - "10.15.3"
branches:
before_script:
  - npm install -g firebase-tools
script:
  - npm run test -- --coverage
  - npm run build
after_success:
  - $TRAVIS_BRANCH = "master" && firebase deploy --token $FIREBASE_TOKEN



